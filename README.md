# LLMs-for-Optimization-Problems
Given the JSSP instance, How to produce a schedule that respects job precedence constraints (operation order within each job) , machine capacity constraints (one operation at a time per machine) and minimizes makespan (total completion time).
